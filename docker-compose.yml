services:
  web:
    build: .
    container_name: landing_django_web
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - media_data:/app/media
      - sqlite_data:/app/db
    restart: unless-stopped

volumes:
  media_data:
  sqlite_data:
